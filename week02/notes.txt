✨ - From Gemini
✅ - Wrote query

When done, rank issues and take top 5 for assignment.

Metadata Issues:
1. No primary key
2. DONATION_ID shouldn't be nullible, should be autonumber.
    2.1. DONATION_ID NUMBER(38,0) AUTOINCREMENT COMMENT 'Unique identifier for each donation.', ✨
3. Organization and Amount probably shouldn't be nullible
4. Age and DOB have inaccuracies and are redundant in this context (Donations from orgs)
5. Rename NAME --> DONOR_NAME and ORGANIZATION --> DONOR_ORG
6. VARCHAR for strings unnecessarily large
7. DATE_OF_BIRTH should be formatted correctly if kept
8. PHONE should have consistent formatting, separate int'l or ext, standardize punctuation
9. ZIP should be string with leading zeroes
10. AMOUNT should be currency

Data Issues:
1. AGE and DATE_OF_BIRTH misaligned (1.1 code below) (✨ to figure out CTE)
2. NAME with inconsistent comma use (2.1 checked proper case)
3. ZIPs should have leading zeroes (3.1 below)
4. CATEGORY has N/As and null (4.1. below)
5. PHONE length inconsistencies (5.1. below)

Honorable Mention:
1. Duplicate data in the table but not quality issue
2. Confirmed all STATE values were in 50 states
3. PHONE with inconsistent use of punctuation
3.1 SELECT PHONE FROM data5035.spring26.donations WHERE NOT REGEXP_LIKE(PHONE, '\\d{3}-\\d{3}-\\d{4}')


1.1         WITH YEAR_FIX AS(
                SELECT *
                    CASE WHEN CAST(YEAR(DATE_OF_BIRTH) AS INT) <= 7 THEN 
                    2000 + CAST(YEAR(DATE_OF_BIRTH) AS INT) ELSE
                    1900 + CAST(YEAR(DATE_OF_BIRTH) AS INT) 
                    END AS BIRTH_YEAR
                FROM data5035.spring26.donations
                )
            
            SELECT *,
            CASE WHEN YEAR(CURRENT_DATE()) - AGE <> BIRTH_YEAR THEN 1 ELSE 0 END AS AGE_CHECK
            FROM YEAR_FIX

2.1         CASE WHEN NAME != INITCAP(NAME) THEN 1 ELSE 0 END AS LOWER_NAMES
3.1         CASE WHEN LENGTH(ZIP) < 5 THEN 1 ELSE 0 END AS SHORT_ZIP
4.1         CASE WHEN (CATEGORY IN ('N/A') OR CATEGORY IS NULL THEN 1 ELSE 0 END) AS NULL_CATS
5.1         CASE WHEN LENGTH(REGEXP_REPLACE(PHONE, '[^0-9]', ''))>10 THEN 1 ELSE 0 END AS PHONE_LEN
6.1         CASE WHEN NAME LIKE ('%,%') THEN 1 ELSE 0 END AS COMMA_NAME